# Base URL of the FastAPI backend.
# Local dev: leave unset, App.jsx falls back to http://127.0.0.1:8000
# Production: set to the deployed Render service URL, e.g.
# VITE_API_URL=https://aerolens-api.onrender.com
VITE_API_URL=
